# Python 缓存
__pycache__/
*.pyc

# YOLO 训练中间产物（最佳权重会复制到 models/ 提交）
runs/

# YOLO 数据集目录（由 scripts/train_yolo.py 依据 processed 数据生成）
data/yolo_dataset/

# 后端运行时数据（SQLite 数据库 + 上传图片，不入库）
app/detection.db
app/uploads/

# Claude Code 本地配置（含机器特定 settings.local.json，不入库）
.claude/
