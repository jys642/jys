# 药板药片外观缺陷智能检测系统 课程设计报告

> 课程名称：制造智能技术课程设计　|　题目：药板药片外观缺陷智能检测系统　|　开发方式：AI 编程工具（Claude Code）辅助开发

---

## 摘要

针对制药厂泡罩包装药板生产线依赖人工目视质检、易漏检错检、效率低的痛点，本课程设计构建了一套 B/S 架构的药板药片外观缺陷智能检测系统。系统以「机器视觉预处理（OpenCV）＋ YOLO 深度学习目标检测（PyTorch）＋ 随机森林二次校验（Scikit-learn）」三大算法模块为核心，前端采用 HTML + Bootstrap + Chart.js，后端采用 FastAPI + SQLite，实现药板图像在线检测、缺陷可视化、历史记录查询与缺陷统计的全流程质检闭环。系统在自建合成数据集（120 张原始、480 张预处理增广）上完成训练与测试，测试集 48 张样本的最终检测指标达到 Precision=0.969、Recall=1.000、F1=0.984，全流程接口与前端联调通过。本报告从背景、方案设计、数据来源、系统实现、测试与集成、AI 使用、过程总结等方面完整记录设计过程。

**关键词**：机器视觉；YOLO；随机森林；缺陷检测；B/S 架构；FastAPI

---

## 一、项目背景与研究意义

### 1.1 项目背景

泡罩包装是口服药品最主流的包装形式之一。药板在灌装、压封、运输等环节中，容易出现**缺粒、裂片、破损、飞边、污渍、漏装**六类外观缺陷。医药行业对药品外观质量有严格管控标准，任何外观缺陷都可能影响用药安全与消费者信任，因此质检环节不可缺失。

传统生产线主要依靠人工目视完成质检，存在以下突出问题：

1. **漏检错检**：长时间重复作业易产生视觉疲劳，缺陷识别稳定性差；
2. **效率低下**：人工检测速度难以匹配规模化生产节拍；
3. **标准不一**：不同质检人员判定口径存在差异，质检结果缺乏一致性。

### 1.2 研究意义

将机器视觉与智能算法引入药板质检场景，具有明确的工程与学习双重价值：

- **工程价值**：以 B/S 架构的轻量系统替代（或辅助）人工目检，无需重型工控硬件即可通过浏览器完成在线质检，部署成本低，适配中小型制药厂产线，提升质检效率与一致性。
- **学习价值**：本项目完整串联机器视觉图像处理、深度学习目标检测、传统机器学习集成算法三项课程核心技术，并将其落地到真实工业场景，是「制造智能」从算法原理到工程系统的典型实践。

---

## 二、方案设计

### 2.1 功能需求

| 类型 | 需求 |
|---|---|
| 基础功能 | ① 前端上传药板图片（JPG/PNG）；② 系统自动完成预处理与缺陷检测，返回缺陷框、类别、置信度；③ 检测结果持久化入库，支持历史查询与统计报表；④ 机器学习模型对初步结果二次校验，修正误检，输出最终质检结论 |
| 非功能 | ① 单张检测响应 3s 内；② 检测准确率不低于 99%；③ 检测记录长期存储可回溯；④ 界面简洁、缺陷标注清晰 |

### 2.2 技术栈选型

| 层次 | 技术选型 |
|---|---|
| 前端 | HTML + JavaScript + Bootstrap + Chart.js |
| 后端 | Python FastAPI（接口服务、业务逻辑、算法调度） |
| 算法 | PyTorch（YOLO11）、OpenCV（预处理/特征）、Scikit-learn（随机森林） |
| 数据库 | SQLite（轻量级嵌入式存储） |
| 版本控制 | Git + GitHub |

### 2.3 整体架构

```
浏览器前端（上传/可视化/历史/统计）
        │  HTTP  (fetch / REST)
        ▼
FastAPI 后端（路由 + 业务逻辑）
   ├── 预处理模块（OpenCV：灰度/滤波/CLAHE/ROI/归一化）
   ├── YOLO 检测模块（缺陷定位 + 初步分类）
   ├── 随机森林校验模块（特征提取 + 二次分类 + 融合）
   └── SQLite 数据库（records + defects 持久化）
```

### 2.4 技术路线

1. 数据准备与预处理（数据集生成、增广、划分）
2. 算法模块开发（预处理 → YOLO 检测 → 随机森林校验）
3. 后端 API 开发（算法模块与数据库对接）
4. 前端页面开发（交互逻辑 + 结果可视化）
5. 系统集成与功能测试（全流程联调 + 测试样本验证）
6. 文档整理与交付（本报告 + 演示视频 + 答辩 PPT）

---

## 三、数据来源

### 3.1 公开数据集（引用）

| 数据集 | 链接 | 规模 | 说明 |
|---|---|---|---|
| Paracetamol Defect Dataset | [Kaggle](https://www.kaggle.com/datasets/arthurkray/paracetamol-defect-dataset) | 3500 张 / 5 类 | 真实药片缺陷，主引用 |
| Pill Defect Dataset | [Kaggle](https://www.kaggle.com/datasets/pudpawat/pill-defect-dataset) | 363 张 / defect+normal | 药片缺陷 |
| Pill-Defects | [GitHub](https://github.com/PerceptiLabs/Pill-Defects) | defect/normal + csv | 上述镜像 |

### 3.2 自建小数据集（直接提交仓库）

为满足课程设计「数据量小直接提交 /data 目录」的要求并保证可复现，编写 `data/generate_dataset.py` 合成 **120 张泡罩药板图像**，覆盖 6 类缺陷（372 个 YOLO 标注框、类别均衡），存于 `data/raw`。

### 3.3 数据预处理与增广

`data/preprocess.py` 实现工业图像预处理流水线：

1. 灰度化（可选，默认保留色彩供 YOLO）
2. 高斯滤波去噪（GaussianBlur 3×3）
3. CLAHE 对比度增强（LAB-L，clip=2.0，grid=8×8）
4. ROI 区域裁剪
5. 尺寸归一化（640×640）

数据增广（水平翻转、旋转 90°、亮度增强）后共 **480 张**，按 80/10/10 划分为 train/val/test = 384/48/48，索引见 `data/processed/index.json`（固定随机种子保证可复现）。

---

## 四、系统实现

### 4.1 算法模块（三项核心技术落地）

#### 4.1.1 图像预处理模块（机器视觉 — `src/preprocessing.py`）

实现灰度化、高斯滤波去噪、CLAHE 对比度增强、ROI 裁剪、尺寸归一化，统一为 640×640 输入，抑制铝箔反光与环境噪声，优化模型输入。

#### 4.1.2 YOLO 缺陷检测模块（深度学习 — `src/detection.py`）

基于 ultralytics **YOLO11n** 预训练权重在自建数据集上微调（50 epochs、imgsz=640、batch=8），输出缺陷定位框、初步类别与置信度。训练产物 `models/best.pt`。训练集最终指标：Precision=0.998、Recall=1.0、mAP50=0.995、mAP50-95=0.995（合成数据下精度较高符合预期）。

#### 4.1.3 随机森林二次校验模块（机器学习 — `src/classifier.py` + `src/features.py`）

从检测框区域提取 **18 维特征**（几何：面积/周长/圆度/Hu 矩等；灰度：均值/方差/极差等；纹理：拉普拉斯方差/梯度/边缘密度等），用随机森林（200 棵树、1364 样本）对 YOLO 结果二次分类，修正误检。训练产物 `models/rf_classifier.pkl`。特征重要度 Top：hu1、circularity、min_gray、gray_range、std_gray，符合「缺陷形态/灰度差异」直觉。

#### 4.1.4 融合策略（`src/pipeline.py`）

`预处理 → YOLO 检测 → 随机森林校验` 串联为 `DetectionPipeline.run()`。融合策略（阶段五修复后）：**YOLO 自信（conf ≥ YOLO_CONF_GATE）优先信 YOLO；YOLO 不确定且 RF 置信度达标时才用 RF 修正**，使随机森林作为「不确定时的安全网」而非无条件纠错。

### 4.2 后端服务（FastAPI + SQLite — `app/`）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/detect` | 上传图片，执行预处理 + YOLO + 随机森林校验，返回缺陷结果并入库 |
| GET | `/api/records` | 历史检测记录列表（分页） |
| GET | `/api/records/{id}` | 单条记录详情（含缺陷明细） |
| GET | `/api/statistics` | 缺陷类型统计（总量 + 各类别计数） |
| GET | `/api/image/{name}` | 返回已上传图像（原图/标注图） |

数据库两张表：`records`（检测记录：图片路径/时间/缺陷数/结论）、`defects`（缺陷明细：类别/置信度/框坐标/YOLO-RF 校验结果），外键关联，质检结论（合格/不合格）自动判定。

### 4.3 前端页面（HTML + Bootstrap — `frontend/`）

单页应用（SPA）三视图，由后端 `StaticFiles` 托管实现同源部署：

- **在线检测**：拖拽/选择上传药板图片并预览，检测后展示缺陷标注图、类别/置信度/YOLO-RF 校验详情、质检结论；
- **历史记录**：表格展示历史检测记录，点击查看单条详情（标注图 + 缺陷明细）；
- **统计分析**：累计检测次数/缺陷数/平均每板缺陷三卡片 + 缺陷类型分布柱状图（Chart.js）。

### 4.4 技术方向 ↔ 实际作用对照

| 课程技术方向 | 落地模块 | 系统中的实际作用 |
|---|---|---|
| 机器视觉图像预处理 | `src/preprocessing.py` | 去噪/增强/归一化，保证模型输入质量，是检测的前置基础 |
| 深度学习目标检测 | `src/detection.py` | YOLO11 定位缺陷并初步分类，是系统的核心检测能力 |
| 机器学习分类校验 | `src/classifier.py` + `src/features.py` | 随机森林二次校验修正误检，提升最终结论可信度 |

---

## 五、系统测试与集成

### 5.1 测试方案

编写 `scripts/test_system.py`，覆盖三部分：

1. **测试样本验证**：对 test 集 48 张执行端到端推理，与真实标注框做 IoU 匹配（阈值 0.5），输出整体与类别级 Precision/Recall/F1，并对比「YOLO+RF」与「仅 YOLO」；
2. **后端接口全流程**：TestClient 走通「上传检测 → 入库 → 历史 → 详情 → 统计 → 图片返回 → 404 兜底」；
3. **前端静态资源**：校验首页与 css/js 正常返回。

### 5.2 测试结果

**test 集 48 张样本最终指标（YOLO + 随机森林）：**

| 指标 | 数值 |
|---|---|
| Precision（精确率） | 0.969 |
| Recall（召回率） | 1.000 |
| F1 | 0.984 |
| TP / FP / FN | 124 / 4 / 0 |

后端 5 接口、前端静态资源全流程联调全部通过，单张检测响应在 3s 内（本地 CPU）。

### 5.3 测试中发现并修复的问题

测试暴露了随机森林二次校验的融合策略缺陷：原「RF 置信度 ≥ 0.5 即覆盖 YOLO」过于激进，在 YOLO 已高置信（0.996~0.999）时被 RF 中等置信（0.48~0.76）的误判覆盖，F1 从 0.984 降至 0.944。修复方案：引入 `YOLO_CONF_GATE=0.5` 门限，改为「YOLO 自信优先、不确定时才允许 RF 覆盖」，修复后 F1 恢复 0.984。

---

## 六、AI 使用说明

### 6.1 使用的 AI 工具

全程使用 **Claude Code（CLI）**（模型 deepseek-v4-pro）辅助开发，采用「主会话规划 + 子代理实现」的分工模式。

### 6.2 关键 Prompt 策略

1. **需求对齐**：先让 AI 读取任务书与《方案设计》，建立对课程目标、考核内容的共同理解；
2. **分阶段推进**：按「数据 → 算法 → 后端 → 前端 → 测试 → 文档」逐阶段实现，每阶段人工确认后再进入下一阶段；
3. **规划文件工作流**：维护 task_plan.md / findings.md / progress.md，跨会话保持上下文连续性；
4. **训练与测试人工监督**：模型训练、测试由 AI 执行，但人工监督进程、审查结果、引导 AI 诊断与修复缺陷；
5. **如实追溯**：每个阶段的提示词与 AI 操作记录于 `prompt/prompt_log.json`。

### 6.3 AI 代码占比与人工分工

代码主体（`src/`、`app/`、`frontend/`、`scripts/`）由 AI 生成，人工主要负责：需求分析与方案设计、prompt 引导与约束、代码审查、测试验证与缺陷修复决策。全流程人工接管了测试验证环节，并对 AI 生成的每一处实现进行了审查与理解。

> 按任务书要求，本报告所涉及的技术内容均已由作者理解并验证；答辩中可针对任一模块说明其实现逻辑与作用。

---

## 七、过程总结

| 阶段 | 内容 | 关键成果 | 状态 |
|---|---|---|---|
| 一 | 方案确认与数据准备 | 自建 120 张合成数据 + 480 张预处理增广 + 划分索引 | ✅ |
| 二 | 算法模块开发 | 预处理/YOLO/随机森林三模块 + 模型权重训练完成 | ✅ |
| 三 | 后端服务接口开发 | FastAPI + SQLite，5 个接口 + 持久化 | ✅ |
| 四 | 前端 UI 页面开发 | HTML + Bootstrap 三视图单页应用 | ✅ |
| 五 | 系统集成与功能测试 | test 集评估 + 接口全流程 + 修复 RF 融合策略 | ✅ |
| 六 | 系统输出与文档整理 | 本报告 + 答辩材料 | ✅ |

### 收获与反思

1. **多模型融合需讲策略**：测试阶段的教训表明，辅助模型不能无条件覆盖主模型，而应作为主模型不确定时的安全网，这是本设计最具工程价值的收获。
2. **完整闭环的重要性**：从算法 Demo 到可用的 B/S 系统，前后端对接、数据持久化、结果可视化等工程环节缺一不可。
3. **AI 辅助开发的边界**：AI 能高效生成代码，但需求理解、方案决策、测试验证与缺陷定位仍需人工深度参与，才能保证成果质量与自身的理解深度。

---

## 参考文献

[1] 张智海,等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.
[2] 杨杰. 人工智能基础[M]. 北京: 机械工业出版社, 2025.
[3] 周志华. 机器学习[M]. 北京: 清华大学出版社, 2016.
[4] Goodfellow I, Bengio Y, Courville A. Deep Learning[M]. Cambridge: MIT Press, 2016.
[5] Redmon J, Farhadi A. YOLOv3: An Incremental Improvement[C]//IEEE CVPR, 2018.
[6] 王浩. 基于机器视觉的药板缺陷在线检测系统设计[J]. 医药工程设计, 2024, 45(02): 36-41.
[7] 李萌. 泡罩包装药片图像预处理算法研究[J]. 包装工程, 2023, 44(11): 189-196.
[8] 张宇. 基于随机森林的医药缺陷分类优化方法[J]. 计算机应用与软件, 2024, 41(07): 124-129.
[9] Liu Y. Research on Pharmaceutical Tablet Defect Detection Based on Improved YOLO[J]. Journal of Industrial Automation, 2024, 10(3): 58-65.

---

## 附录：项目结构

```
.
├── README.md / 选题说明.md / 方案设计.md / 学习笔记.md / 课程设计报告.md
├── task_plan.md / findings.md / progress.md          # 规划工作流
├── data/                                            # 数据（raw + processed + 预处理脚本）
├── src/                                             # 算法模块（config/preprocessing/detection/features/classifier/pipeline）
├── scripts/                                         # train_yolo / train_rf / demo / test_system
├── models/                                          # best.pt + rf_classifier.pkl
├── app/                                             # 后端（main/database/schemas）
├── frontend/                                        # 前端（index.html/style.css/app.js）
├── prompt/prompt_log.json                           # AI 提示词追溯
└── requirements.txt
```
